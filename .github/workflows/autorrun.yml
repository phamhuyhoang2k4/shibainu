name: Auto Task Example

on:
  push:           # Kích hoạt khi có push code
    branches:
      - main      # Chỉ chạy trên nhánh main
  workflow_dispatch: # Cho phép bấm nút "Run workflow" thủ công

jobs:
  run-task:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run auto task
        run: |
          echo "🚀 Hello from GitHub Actions!"
          date
          echo "Mình vừa chạy xong một tác vụ hợp pháp 😉"
